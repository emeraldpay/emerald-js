{
  "private": true,
  "scripts": {
    "test": "CI=true lerna run test --stream",
    "test:coverage": "CI=true lerna run test:coverage --stream"
	},
  "devDependencies": {
    "lerna": "^3.10.5"
  }
}
